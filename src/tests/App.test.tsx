import 'react-native'
import React from 'react'

it('works', () => {
  expect(1).toBe(1)
})

// TODO: fix relay tests
// it('renders without crashing', () => {
//   const rendered = renderer.create(<App/>).toJSON()
//   expect(rendered).toBeTruthy()
// })
